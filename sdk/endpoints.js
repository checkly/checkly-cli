const ENDPOINTS = {
  CHECKS: '/checks',
  ACCOUNT: '/account',
  LOCATIONS: '/locations',
  CHECK_STATUSES: '/check-statuses',
  SIGNED_URL: '/sockets/signed-url',
}

module.exports = {
  ...ENDPOINTS,
}
