const CHECK = {
  name: 'check',
  directory: 'checks',
}

const GROUP = {
  name: 'group',
  directory: 'groups',
}

const ALERT_CHANNEL = {
  name: 'alert-channel',
  directory: 'alert-channels',
}

module.exports = {
  CHECK,
  GROUP,
  ALERT_CHANNEL,
}
